services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      
    volumes:
      - ./www:/var/www/html
      - db:/var/lib/mysql

    ports:
      - "${APP_PORT}:80"

    restart: unless-stopped

volumes:
  db:
    driver: local
    