[supervisord]
nodaemon=true

[program:apache]
command=/bin/bash -c "source /etc/apache2/envvars && apache2 -D FOREGROUND"
autostart=true
autorestart=true
priority=10

[program:mysql]
command=mysqld_safe
autostart=true
autorestart=true
priority=20
